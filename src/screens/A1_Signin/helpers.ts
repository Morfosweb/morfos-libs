import { setData } from '../../../lib/central-data';

export const prodsList = async () => {
  const getProdBase = 'await firestore...';
  const getProdShop = 'await firestore...';

  const setMergeData = 'forin...';

  return setData({ A1: { listProds: 'setMergeData' } });
};
